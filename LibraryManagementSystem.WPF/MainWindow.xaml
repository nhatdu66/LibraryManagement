<Window x:Class="LibraryManagementSystem.WPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:views="clr-namespace:LibraryManagementSystem.WPF.Views"
        Title="Quản Lý Thư Viện" Height="650" Width="950"
        WindowStartupLocation="CenterScreen">

	<!-- Khai báo BooleanToVisibilityConverter ở đây -->
	<Window.Resources>
		<BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
	</Window.Resources>

	<Grid Margin="10">
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="*"/>
		</Grid.RowDefinitions>

		<!-- Header -->
		<StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,10">
			<TextBlock Text="{Binding WelcomeMessage}" 
                       FontSize="20" 
                       FontWeight="SemiBold" 
                       Foreground="DarkBlue"
                       VerticalAlignment="Center"/>

			<Button Content="Đăng xuất" 
                    Command="{Binding LogoutCommand}" 
                    Visibility="{Binding IsLoggedIn, Converter={StaticResource BooleanToVisibilityConverter}}"
                    Margin="30,0,0,0" 
                    Width="100" Height="32"/>
		</StackPanel>

		<TabControl Grid.Row="1" SelectedIndex="{Binding SelectedTabIndex}">
			<TabItem Header="Đăng nhập">
				<views:LoginView DataContext="{Binding LoginVM}"/>
			</TabItem>

			<TabItem Header="Danh sách sách">
				<views:BookCatalogView />
			</TabItem>

			<TabItem Header="Mượn Sách" >
				<views:BorrowView DataContext="{Binding BorrowVM}"/>
			</TabItem>

			<TabItem Header="Tài khoản" IsEnabled="{Binding IsLoggedIn}">
				<TextBlock Text="Chức năng đang phát triển" Margin="30" FontSize="14"/>
			</TabItem>
		</TabControl>
	</Grid>
</Window>