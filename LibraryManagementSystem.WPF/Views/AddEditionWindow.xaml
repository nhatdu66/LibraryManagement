<Window x:Class="LibraryManagementSystem.WPF.Views.AddEditionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Add Book Edition" Height="420" Width="450"
        WindowStartupLocation="CenterOwner">

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="140"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Text="Work ID:" Grid.Row="0" VerticalAlignment="Center"/>
        <TextBox x:Name="txtWorkId" Grid.Row="0" Grid.Column="1" Margin="6"/>

        <TextBlock Text="ISBN:" Grid.Row="1" VerticalAlignment="Center"/>
        <TextBox x:Name="txtISBN" Grid.Row="1" Grid.Column="1" Margin="6"/>

        <TextBlock Text="Publisher:" Grid.Row="2" VerticalAlignment="Center"/>
        <ComboBox x:Name="cmbPublisher" Grid.Row="2" Grid.Column="1" Margin="6"
                  IsEditable="True"
                  DisplayMemberPath="PublisherName"
                  SelectedValuePath="PublisherId"
                  IsTextSearchEnabled="True"/>

        <TextBlock Text="Publish Year:" Grid.Row="3" VerticalAlignment="Center"/>
        <TextBox x:Name="txtPublishYear" Grid.Row="3" Grid.Column="1" Margin="6"/>

        <TextBlock Text="Edition Number:" Grid.Row="4" VerticalAlignment="Center"/>
        <TextBox x:Name="txtEditionNumber" Grid.Row="4" Grid.Column="1" Margin="6"/>

        <TextBlock Text="Language:" Grid.Row="5" VerticalAlignment="Center"/>
        <TextBox x:Name="txtLanguage" Grid.Row="5" Grid.Column="1" Margin="6"/>

        <!-- Buttons -->
        <StackPanel Grid.Row="7" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="6">
            <Button Content="Save" Width="100" Margin="6" Click="btnSave_Click"/>
            <Button Content="Cancel" Width="100" Margin="6" Click="btnCancel_Click"/>
        </StackPanel>

    </Grid>
</Window>