<UserControl x:Class="LibraryManagementSystem.WPF.Views.BookCatalogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

	<Grid Margin="10">
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<!-- Debug info -->
			<RowDefinition Height="Auto"/>
			<!-- Tìm kiếm -->
			<RowDefinition Height="*"/>
			<!-- Danh sách sách -->
		</Grid.RowDefinitions>

		<!-- Debug: Hiển thị số lượng sách và thông báo lỗi -->
		<StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
			<TextBlock Text="{Binding Books.Count, StringFormat='Số sách: {0}'}" 
                       FontSize="16" FontWeight="Bold" Foreground="Red" Margin="0,0,20,0"/>

			<TextBlock Text="{Binding StatusMessage}" 
                       FontSize="14" Foreground="DarkRed" TextWrapping="Wrap" MaxWidth="400"/>
		</StackPanel>

		<!-- Phần tìm kiếm -->
		<StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
			<TextBox Width="300" Margin="0,0,10,0" 
                     Text="{Binding SearchKeyword, UpdateSourceTrigger=PropertyChanged}"/>
			<Button Content="Tìm kiếm" Command="{Binding SearchCommand}" Width="100"/>
			<Button Content="Hiển thị tất cả" Command="{Binding LoadAllCommand}" Margin="10,0,0,0"/>
		</StackPanel>

		<!-- Danh sách sách - Thêm Border đỏ để debug -->
		<Border Grid.Row="2" BorderBrush="Red" BorderThickness="2" Background="LightYellow" Margin="0,10,0,0">
			<ListView ItemsSource="{Binding Books}" BorderThickness="0">
				<ListView.View>
					<GridView>
						<GridViewColumn Header="Tiêu đề" DisplayMemberBinding="{Binding Title}" Width="250"/>
						<GridViewColumn Header="Tác giả" DisplayMemberBinding="{Binding AuthorsString}" Width="180"/>
						<GridViewColumn Header="Thể loại" DisplayMemberBinding="{Binding CategoriesString}" Width="150"/>
						<GridViewColumn Header="Còn lại" DisplayMemberBinding="{Binding AvailableCopies}" Width="80"/>
						<GridViewColumn Header="Tổng" DisplayMemberBinding="{Binding TotalCopies}" Width="80"/>
					</GridView>
				</ListView.View>
			</ListView>
		</Border>
	</Grid>
</UserControl>