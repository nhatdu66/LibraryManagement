<UserControl x:Class="LibraryManagementSystem.WPF.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:helpers="clr-namespace:LibraryManagementSystem.WPF.Helpers"
			 mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="400">

	<Grid Margin="30">
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="*"/>
		</Grid.RowDefinitions>

		<TextBlock Text="ĐĂNG NHẬP" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,30"/>

		<StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,20">
			<RadioButton Content="Độc giả (bạn đọc)" IsChecked="{Binding IsReaderLogin}" GroupName="AccountType" Margin="0,0,30,0"/>
			<RadioButton Content="Nhân viên" IsChecked="{Binding IsEmployeeLogin}" GroupName="AccountType"/>
		</StackPanel>

		<StackPanel Grid.Row="2" Margin="0,0,0,15">
			<TextBlock Text="Email" Margin="0,0,0,5"/>
			<TextBox Text="{Binding Email}" Width="300" HorizontalAlignment="Left"/>
		</StackPanel>

		<StackPanel Grid.Row="3" Margin="0,0,0,20">
			<TextBlock Text="Mật khẩu" Margin="0,0,0,5"/>
			<PasswordBox x:Name="txtPassword" 
             Margin="0,10,0,0" 
             Width="250"/>
		</StackPanel>

		<!-- Button dùng CommandParameter -->
		<Button Grid.Row="4" Content="Đăng nhập" 
        Command="{Binding LoginCommand}" 
        CommandParameter="{x:Reference txtPassword}"
        Width="140" Height="40" HorizontalAlignment="Left" Margin="0,0,0,10"/>

		<TextBlock Grid.Row="5" Text="{Binding StatusMessage}" 
                   Foreground="Red" TextWrapping="Wrap" Margin="0,10,0,0"/>
	</Grid>
</UserControl>