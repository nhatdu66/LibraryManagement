<UserControl x:Class="LibraryManagementSystem.WPF.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

	<Grid Margin="20">
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="*"/>
		</Grid.RowDefinitions>

		<TextBlock Text="ĐĂNG NHẬP" FontSize="24" HorizontalAlignment="Center" Margin="0,0,0,30"/>

		<StackPanel Grid.Row="1" Margin="0,0,0,15">
			<TextBlock Text="Email" Margin="0,0,0,5"/>
			<TextBox Text="{Binding Email}" Width="300" HorizontalAlignment="Left"/>
		</StackPanel>

		<StackPanel Grid.Row="2" Margin="0,0,0,20">
			<TextBlock Text="Mật khẩu" Margin="0,0,0,5"/>
			<PasswordBox x:Name="txtPassword" Width="300" HorizontalAlignment="Left"/>
		</StackPanel>

		<Button Grid.Row="3" Content="Đăng nhập" Command="{Binding LoginCommand}" 
                Width="120" Height="35" HorizontalAlignment="Left" Margin="0,0,0,10"/>

		<TextBlock Grid.Row="4" Text="{Binding StatusMessage}" 
                   Foreground="Red" TextWrapping="Wrap" Margin="0,10,0,0"/>
	</Grid>
</UserControl>