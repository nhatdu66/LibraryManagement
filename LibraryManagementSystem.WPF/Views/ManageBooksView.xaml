<UserControl x:Class="LibraryManagementSystem.WPF.Views.ManageBooksView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="550" d:DesignWidth="800">

	<Grid Margin="10">
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="*"/>
			<RowDefinition Height="Auto"/>
		</Grid.RowDefinitions>

		<!-- Tiêu đề -->
		<TextBlock Text="Quản lý sách" FontSize="20" FontWeight="Bold" Margin="0,0,0,10"/>

		<!-- Thanh tìm kiếm (giữ giống tab danh sách) -->
		<StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
			<TextBox Width="300" Text="{Binding SearchKeyword, UpdateSourceTrigger=PropertyChanged}" 
                     Margin="0,0,10,0" Padding="5"/>
			<Button Content="Tìm kiếm" Command="{Binding SearchCommand}" Width="100"/>
			<Button Content="Làm mới" Command="{Binding RefreshCommand}" Width="100" Margin="10,0,0,0"/>
		</StackPanel>

		<!-- Danh sách sách (giống hệt tab kia) -->
		<Border Grid.Row="1" BorderBrush="Gray" BorderThickness="1">
			<ListView ItemsSource="{Binding Books}">
				<ListView.View>
					<GridView>
						<GridViewColumn Header="ID" DisplayMemberBinding="{Binding WorkId}" Width="60"/>
						<GridViewColumn Header="Tên sách" DisplayMemberBinding="{Binding Title}" Width="250"/>
						<GridViewColumn Header="Tác giả" DisplayMemberBinding="{Binding AuthorsString}" Width="180"/>
						<GridViewColumn Header="Thể loại" DisplayMemberBinding="{Binding CategoriesString}" Width="150"/>
						<GridViewColumn Header="Còn lại" DisplayMemberBinding="{Binding AvailableCopies}" Width="80"/>
						<GridViewColumn Header="Tổng" DisplayMemberBinding="{Binding TotalCopies}" Width="80"/>
					</GridView>
				</ListView.View>
			</ListView>
		</Border>

		<!-- Trạng thái -->
		<TextBlock Grid.Row="2" Text="{Binding StatusMessage}" 
                   Margin="0,10,0,0" Foreground="DarkBlue" TextWrapping="Wrap"/>
	</Grid>
</UserControl>